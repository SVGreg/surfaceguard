# Binaries
/surfaceguard
/dist/
*.exe
*.test
*.out

# Signatures & keys produced during local runs
*.skillsig
*.key
/keys/
# ...but do commit attestations for this repo's own shipped skills
!.claude/skills/**/*.skillsig

# Hook runtime artifacts (config is intentionally committable for shared enforcement)
surfaceguard-hook.log
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Go
/vendor/

# Evaluation artifacts
/evaluation/anthropic/
/evaluation/clawhub/
/evaluation/clawhub*/
/evaluation/skillsmp/
/evaluation/skillssh/
/evaluation/orgs/
/evaluation/aws/
/evaluation/skillject/
/evaluation/reports/

# Maintenance-loop runtime state (per-machine; not shared)
/.claude/maintenance/
# Development-loop runtime state (per-machine; not shared)
/.claude/development/
